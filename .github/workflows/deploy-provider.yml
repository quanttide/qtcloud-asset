name: Build and Push Provider Image

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/deploy-provider.yml"
      - "manifests/docker/Dockerfile.provider"
      - "src/provider/**"
  pull_request:
    branches: [main]
    paths:
      - ".github/workflows/deploy-provider.yml"
      - "manifests/docker/Dockerfile.provider"
      - "src/provider/**"

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Alibaba Cloud Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: crpi-uorshhk4a32pmmio.cn-hangzhou.personal.cr.aliyuncs.com
          username: ${{ secrets.ALIYUN_REGISTRY_USERNAME }}
          password: ${{ secrets.ALIYUN_REGISTRY_PASSWORD }}

      - name: Build provider image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: manifests/docker/Dockerfile.provider
          platforms: linux/amd64
          push: ${{ github.event_name == 'push' }}
          provenance: false
          sbom: false
          tags: |
            crpi-uorshhk4a32pmmio.cn-hangzhou.personal.cr.aliyuncs.com/quanttide/qtcloud-asset-provider:latest
            crpi-uorshhk4a32pmmio.cn-hangzhou.personal.cr.aliyuncs.com/quanttide/qtcloud-asset-provider:${{ github.sha }}
