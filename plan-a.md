# Plan A：账号与门禁最小落地方案

> 对应 `ROADMAP.md` 的 `0.1.1`。  
> 目标：先把敏感接口锁住，再补账号管理。

## 背景

- 站点已上线 `asset.cloud.quanttide.com`
- Provider 仍是公开 REST 接口
- Studio 目前只是前端隐藏私密入口，不能作为安全边界
- 敏感接口需要尽快加账号与权限

## 目标

- 未登录不能访问敏感 API
- 私密桶只给授权人员
- 生成对象链接可控、可审计
- 所有敏感操作可追踪、可撤销

## 原则

- 后端鉴权，前端只展示状态
- 优先 SSO，次选邀请码 / 邮箱验证码
- 不做自注册，不做密码体系
- 会话使用 `HttpOnly` Cookie
- CORS 只放行登记过的来源
- 敏感接口加限流和审计

## 项目边界与依赖

- `viewer` 对应内部只读用户，`admin` 对应内部管理用户，先不做更多复杂分级
- `IP allowlist`、网关白名单、SSO 接入属于平台协作项，不是本仓库单独完成的内容
- `users`、`sessions`、`audit_logs` 需要一个受管持久层承载，优先平台共享 RDS 或同类受管存储
- 账号系统只负责访问门禁，不改变本项目“只读资产发现 + 全景展示”的主定位

## 7 天执行计划

> 执行要求：下面所有动作项都按 checklist 维护，执行完成后立刻把 `[ ]` 改成 `[x]`，并在同级补一句结果。

### 第 1 天：定门槛

目标是先明确“谁能进、进来之后能看什么”。

- [ ] 确认身份源：飞书/Lark SSO 优先，若暂时接不通，则用邀请码 + 邮箱验证码兜底
- [ ] 确认角色模型：先只保留 `viewer`、`admin`，不要一开始就做复杂分级
- [ ] 确认访问边界：哪些接口公开、哪些接口必须登录、哪些接口仅管理员可见
- [ ] 配置临时 IP allowlist 或网关白名单，给后续改造留缓冲
- [ ] 输出《访问矩阵》：接口名、访问条件、角色、是否记录审计
- [ ] 输出《依赖清单》：平台白名单、SSO 接入、受管存储、灰度发布窗口

验收标准：

- [ ] 敏感接口清单定下来
- [ ] 角色边界定下来
- [ ] 临时阻断策略定下来

### 第 2 天：后端登录

目标是让 Provider 先“认人”，而不是先改页面。

- [ ] 新增认证模块，补 `/auth/login`、`/auth/callback`、`/auth/logout`、`/auth/me`
- [ ] 采用服务端会话，不把 token 放到前端存储里
- [ ] 会话通过 `HttpOnly` Cookie 下发，必要时加 `Secure`、`SameSite`
- [ ] CORS 改成精确 origin 白名单，跨域请求需要时启用 credentials
- [ ] 未登录访问敏感接口直接返回 401

验收标准：

- [ ] 未登录请求不能打到业务数据
- [ ] 登录后刷新页面仍保持会话
- [ ] 退出登录后会话失效

### 第 3 天：存储与审计

目标是让账号系统有可追踪、可撤销的落点。

- [ ] 建最小数据模型：`users`、`sessions`、`audit_logs`
- [ ] `users` 记录外部身份标识、邮箱、姓名、角色、状态
- [ ] `sessions` 记录会话 ID、用户 ID、过期时间、撤销状态、IP、UA
- [ ] `audit_logs` 记录谁在什么时间对什么资源做了什么操作
- [ ] 支持管理员封禁账号、撤销会话

验收标准：

- [ ] 管理员能让某个账号立即失效
- [ ] 每个敏感操作都能找到审计记录
- [ ] 重启服务后数据不丢

### 第 4 天：业务鉴权

目标是把权限真正落到业务接口。

- [ ] 给 `/buckets`、`/objects`、`/object-url` 加统一鉴权
- [ ] 公开桶和私密桶分开处理，私密桶默认只给授权角色
- [ ] `object-url` 生成前先做角色校验，不通过就直接拒绝
- [ ] 把“能看列表”和“能拿链接”拆成两层权限，避免过度放开

验收标准：

- [ ] 未授权用户看不到私密桶内容
- [ ] 未授权用户拿不到签名链接
- [ ] 403 与 401 的返回边界清晰

### 第 5 天：管理能力

目标是让账号不靠手改数据库。

- [ ] 做最小管理页或 CLI，支持邀请、禁用、改角色
- [ ] 管理员可以查看用户最后登录时间和当前状态
- [ ] 用户管理动作自动写审计日志
- [ ] 如果时间紧，先做后端管理接口，再补 UI

验收标准：

- [ ] 管理员能独立完成账号开通和停用
- [ ] 不需要直接操作数据库
- [ ] 管理动作可追踪

### 第 6 天：Studio 接入

目标是把登录态接到前端体验里，但不把前端变成安全边界。

- [ ] Studio 启动时先调 `/auth/me`
- [ ] 未登录自动跳转登录
- [ ] 页面层面显示当前用户状态和退出登录入口
- [ ] 移除任何敏感 token 的前端持久化逻辑
- [ ] 把未授权状态、登录中状态、登录失败状态都补齐

验收标准：

- [ ] 浏览器里不保存敏感 token
- [ ] 未登录用户进不了数据页
- [ ] 登录、退出、刷新三种状态都正常

### 第 7 天：测试发布

目标是让这套东西能安全上线。

- [ ] 补单测：认证、鉴权、会话失效、审计记录
- [ ] 补集成测试：CORS、CSRF、限流、登录链路、权限边界
- [ ] 补 E2E：未登录、普通用户、管理员三种路径
- [ ] 生产前做一次灰度发布，先小范围验证
- [ ] 检查日志、告警、错误页，确认没有敏感信息泄露

验收标准：

- [ ] 关键路径测试全部通过
- [ ] 线上访问路径可追踪
- [ ] 灰度期间没有权限绕过或跨域异常

## 最小数据模型

- `users(id, external_id, email, name, role, status, created_at, last_login_at)`
- `sessions(id, user_id, expires_at, revoked_at, ip, user_agent)`
- `audit_logs(id, user_id, action, target, result, ip, created_at)`

## API 端点

- `GET /auth/me`
- `GET /auth/login`
- `GET /auth/callback`
- `POST /auth/logout`
- `GET /buckets`
- `GET /buckets/{name}/objects`
- `GET /buckets/{name}/object-url`

## 风险与处理

- 跨域 Cookie：如果继续用 `asset.cloud.quanttide.com` 调 `api.quanttide.com`，必须收紧 CORS 并启用 credentials
- 会话失效：要支持封禁和撤销会话，不能只靠前端退出
- 身份源延期：如果 SSO 还没接通，先用邀请码 / 邮箱验证码兜底
- 权限错位：所有判定必须在 Provider，不能只放在 Studio

## 验收标准

- 未登录用户无法访问敏感 API
- 私密桶不暴露对象内容和签名 URL
- 账号可邀请、禁用、改角色
- 所有敏感行为都有审计记录
- 关键路径测试通过并可灰度发布
