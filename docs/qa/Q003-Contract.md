# Q003: 契约体系

**[ 需求来源 ]**：`PRD/Contract` 定义契约为"声明式资产地图"，不管理文件列表。
**[ 核心契约 ]**：`.quanttide/asset/contract.yaml`

---

## 1. 现实需求：契约声明资产组成
* **验收点**：契约包含 `assets` 和 `skills` 两段，每项有 `type`、`category`、`path`。
* **验证资产**：`.quanttide/asset/contract.yaml`
* **状态**：✅ *Passed*

## 2. 现实需求：契约不管理文件列表
* **验收点**：契约只声明根路径和分类，不列举具体文件。
* **验证资产**：`docs/add/contract.md` — "契约设计结论"
* **状态**：✅ *Passed*

## 3. 现实需求：契约解析为可执行技能
* **验收点**：`skills.*.commands` 可从契约中读取并执行。
* **验证资产**：`src/cli/`（规划中，尚未实现解析器）
* **状态**：⏸️ *Pending*

## 4. 现实需求：契约验证资产路径可达
* **验收点**：契约中声明的 `path` 实际存在于文件系统中。
* **验证资产**：待实现验证脚本
* **状态**：⏸️ *Pending*

## 5. 现实需求：契约版本与代码版本对齐
* **验收点**：契约 `version` 字段与 git tag 对应。
* **验证资产**：待实现契约 diff 工具
* **状态**：⏸️ *Pending*
