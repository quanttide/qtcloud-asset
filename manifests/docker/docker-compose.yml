version: '3.8'

services:
  studio:
    build:
      context: ..
      dockerfile: manifests/docker/Dockerfile
    ports:
      - "8080:80"
    restart: unless-stopped
    networks:
      - app-network

  # 后端服务（预留）
  # backend:
  #   build:
  #     context: ..
  #     dockerfile: manifests/docker/Dockerfile.backend
  #   ports:
  #     - "8081:8080"
  #   environment:
  #     - ALIBABA_CLOUD_ACCESS_KEY_ID=${ALIBABA_CLOUD_ACCESS_KEY_ID}
  #     - ALIBABA_CLOUD_ACCESS_KEY_SECRET=${ALIBABA_CLOUD_ACCESS_KEY_SECRET}
  #   depends_on:
  #     - redis
  #   networks:
  #     - app-network

  # Redis 缓存（预留）
  # redis:
  #   image: redis:alpine
  #   ports:
  #     - "6379:6379"
  #   networks:
  #     - app-network

networks:
  app-network:
    driver: bridge
